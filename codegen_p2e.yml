overwrite: true
schema: "https://lucis-ms.koolab.io/graphql"
# documents: "src/**/*.graphql"
generates:
  src/generated/graphql_p2e.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"

# -----> @graphql-codegen/introspection
#  ./graphql.schema.json:
#    plugins:
#      - "introspection"
